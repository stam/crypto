import styled from 'styled-components';

const Header = styled.h1`
  font-size: 5em;
  margin: 0;
  margin-bottom: 1rem;
  padding: 1rem;
  padding-bottom: 8px;
  grid-column: 1 / -1;
`;

export default Header;
